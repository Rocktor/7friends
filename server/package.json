{
  "name": "@zpy/server",
  "version": "0.1.0",
  "description": "找朋友升级 对局服务器（生产级）— 注册登录/单桌/WebSocket/强AI/持久化",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "start": "node --experimental-sqlite dist/server.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "ws": "^8.21.0"
  }
}
